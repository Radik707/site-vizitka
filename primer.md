# primer.md

## Сейчас
Сайт переработан в продающее портфолио под фриланс и ЗАДЕПЛОЕН.
- **Живой адрес: https://radik707.github.io/site-vizitka/** (GitHub Pages, ветка main, репо публичный).
- portfolio.html: 7 реальных кейсов (фейковые удалены). Первые два — флагманы «под заказ»:
  CRM на EspoCRM (медтех, 152-ФЗ) и B2B-каталог торгпреда (Next.js, Vercel, 935 товаров).
- index.html: заголовок-польза в hero, секция «Избранные кейсы» (CRM / каталог / дайджест),
  услуга «CRM и веб-системы», блок «Что вы получаете в любом случае» (честные гарантии).
- Секция «Отзывы» свёрстана, но СКРЫТА в HTML-комментарии (реальных отзывов пока нет) —
  раскрыть, когда наберётся ≥2 настоящих: убрать обёртку-комментарий и заменить плейсхолдеры.
- В конце .portfolio-list — закомментированный ШАБЛОН кейса: копируй блок выше комментария,
  меняй num/data-category(automation|website|bot)/тег/заголовок/стек/Задачу/Решение/факты.
- Полное ТЗ работы: ЗАДАЧА-портфолио-под-фриланс.md. ⚠️ Честность: метрики не выдумывать.

## Скриншоты кейсов
- Добавлены в кейсы CRM (01), каталог (02), Telegram-бот (04): `images/cases/*.jpg`.
- Обработаны через System.Drawing: ресайз под веб, на CRM замазан бренд «BIOPATRIOT» (зелёный в зелёный).
- ⚠️ Исходные `images/cases/*.png` — в `.gitignore` (бренды/UUID/ПДн), в публичный репо НЕ идут.
- Чтобы добавить скрин в кейс: положить .jpg в `images/cases/`, вставить
  `<img class="portfolio-case__shot" src="images/cases/ИМЯ.jpg" alt="...">` в начало `.portfolio-case__right`
  (для вертикальных скринов телефона добавить класс `portfolio-case__shot--phone`).

## Следующее (по желанию Олега)
- CRM: когда наполнится — заменить скрин на доску «Сделки» (7 этапов, без ПДн).
- Собрать первые реальные отзывы → раскрыть секцию «Отзывы».

## История
- **2026-06-07** — Скриншоты в кейсы CRM/каталог/бот (бренд клиента замазан, исходники в gitignore); правка CRM: воронка 7 этапов вместо 14.
- **2026-06-07** — Редизайн под фриланс: убраны 4 выдуманных кейса, добавлены 7 реальных
  (флагманы CRM + B2B-каталог), продающая главная (заголовок-польза, избранные кейсы,
  гарантии), скрытый каркас отзывов, шаблон кейса. Деплой на GitHub Pages, репо сделан публичным.
- **2026-04-29** — Фаза 4: aurora «дыхание» в hero, shimmer на акцентном слове, scroll reveal (IntersectionObserver + stagger).
- **2026-04-29** — split-кнопки Телеграм/Макс по всему сайту, иконки мессенджеров в карточках, контекстные CTA.
- **2026-04-28** — Фаза 3: portfolio.html (кейсы + фильтр) и contacts.html (Telegram, Max, ВКонтакте).
- **2026-04-28** — Фаза 2: Тарифы (3 пакета + сопровождение), FAQ (аккордеон), Финальный CTA, секции Подход/Кейсы/Стек/Процесс.
- **2026-04-26** — Фаза 1 редизайна: CSS-переменные (тёмная палитра + золото + фиолет), Playfair Display + Inter, hero с aurora, карточки услуг 2×2.
- **2026-04-26** — инициализация проекта, созданы все страницы (index, about, contacts, portfolio, services).
