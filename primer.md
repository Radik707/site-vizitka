# primer.md

## Сейчас
Фаза 4 завершена — добавлены все три анимации:
- **Aurora «дыхание»**: `.hero::before` — radial-gradient фиолет+золото с CSS animation 12s ease-in-out infinite, плавно пульсирует.
- **Shimmer**: `.hero__title .accent::after` — скользящий блик по акцентному слову каждые 4.5s.
- **Scroll reveal**: класс `.reveal` + JS IntersectionObserver в `scripts/animations.js`. Stagger через `.reveal-group`. Применено на всех трёх страницах. Уважает `prefers-reduced-motion`.
Проект завершён в части дизайна и анимаций.

## История
- **2026-04-29** — Фаза 4: aurora «дыхание» в hero, shimmer на акцентном слове, scroll reveal на всех страницах (IntersectionObserver + stagger).
- **2026-04-29** — split-кнопки Телеграм/Макс по всему сайту, иконки мессенджеров в карточках услуг и кейсах, контекстные CTA («Заказать сайт», «Обсудить подобный кейс» и др.)
- **2026-04-28** — Фаза 3: portfolio.html (4 кейса + фильтр) и contacts.html (Telegram, Max, ВКонтакте + шаги после обращения). Правки текстов на contacts.html.
- **2026-04-28** — Фаза 2 (завершена): Тарифы (3 пакета + сопровождение от 3 000 ₽/мес), FAQ (аккордеон, 5 вопросов), Финальный CTA.
- **2026-04-28** — Фаза 2 (частично): секции Подход, Кейсы, Стек, Процесс.
- **2026-04-26** — Фаза 1 редизайна: CSS-переменные (тёмная палитра + золото + фиолет), шрифты Playfair Display + Inter, новые кнопки, hero с aurora-эффектом, 4 карточки услуг 2×2.
- **2026-04-26** — инициализация проекта, созданы все страницы (index, about, contacts, portfolio, services).
